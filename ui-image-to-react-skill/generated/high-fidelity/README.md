# High-Fidelity React Implementation

This folder contains the high-fidelity React implementation of the provided image. The implementation follows the `high-fidelity-motherduck` workflow described in the `ui-image-to-react` skill.

## Preview

Run:

```bash
npm install
npm run dev
```

Then open the local Vite URL shown in the terminal.
